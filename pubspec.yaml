name: mercadosmart
description: O jeito inteligente de fazer compras. Lista por setor + reconhecimento por IA.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  http: ^1.1.0
  image_picker: ^1.0.4
  shared_preferences: ^2.2.2
  google_mlkit_text_recognition: ^0.11.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_riverpod: ^3.3.2
  uuid: ^4.5.3
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
